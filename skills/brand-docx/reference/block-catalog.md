# Block Catalog

M1 supports `heading`, `paragraph`, `callout`, `list`, `table`, and `pagebreak`
for DOCX generation. The wider IntermediateDocument model is already present in
`scripts/brandkit/ir/model.py`.
